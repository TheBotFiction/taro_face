# frozen_string_literal: true

module Types
  RSpec.describe QuestionType do
    subject { QuestionType }

    # it { is_expected.to have_field(:id).that_returns(GraphQL::Types::ID) }
    # it { is_expected.to have_field(:term).that_returns(GraphQL::Types::String) }
    # it { is_expected.to have_field(:phrase).that_returns(GraphQL::Types::String) }
    # it { is_expected.to have_field(:answers).that_returns([GraphQL::Types::String]) }
  end
end
