# frozen_string_literal: true

# module Types
#   RSpec.describe Term do
#     # avail type definer in our tests
#     # types = GraphQL::Define::TypeDefiner.instance
#     subject { Term.fields }
#
#     it 'has an :id field of ID type' do
#       # Ensure that the field id is of type ID
#       expect(subject).to have_field(:id).that_returns(BaseObject::ID)
#     end
#
#     it 'has a :term field of String type' do
#       # Ensure the field is of String type
#       expect(subject).to have_field(:term).that_returns(String)
#     end
#   end
# end
